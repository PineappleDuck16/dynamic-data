<form action="">


{{#each data.fields}}
<form>
  <div class="mb-3">
    <label for="exampleInputEmail1" class="form-label">{{this.value}}</label>
    <input type="{{this.type}}" name="{{this.name}}" class="form-control" id="{{this.name}}">
  </div>
</form>
{{/each}}

<button type="submit" class="btn btn-primary">Go Mad</button>

</form>