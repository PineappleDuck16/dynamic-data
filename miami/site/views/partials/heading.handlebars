
{{!-- this "if" is a helper to evalutate if teh variable or experssion exists--}}
{{#if data.image}}
<img class="hero img-fluid" src="{{data.image}}" alt="">
{{/if}}

<h1 class="primary-heading">{{data.title}}</h1>

<p class="abstract">
{{data.abstract}}
</p>